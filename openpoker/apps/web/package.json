{
  "name": "@openpoker/web",
  "version": "0.0.1",
  "private": true,
  "description": "OpenPoker Web App — Pure HTML/JS + WASM",
  "scripts": {
    "serve": "python -m http.server 8080 --directory .",
    "serve:py3": "python3 -m http.server 8080 --directory .",
    "serve:node": "npx http-server -p 8080"
  }
}
