{
  "name": "@digital-nomad-cn/api",
  "version": "1.0.0",
  "description": "Digital Nomad CN REST API v1 - Cloudflare Workers",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --outdir=dist --format=esm --platform=neutral",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240614.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5",
    "wrangler": "^3.60.0"
  }
}
